# Start a django project

```shell
mkdir projects
cd projects
mkdir env
# create a virtual env
python -m venv env/projectname
# activate the virtual env
source env/projectname/bin/activate
# install django
pip install django
# create a django project
django-admin startproject projectname


```